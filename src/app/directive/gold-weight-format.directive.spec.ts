import { GoldWeightFormatDirective } from './gold-weight-format.directive';

describe('GoldWeightFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new GoldWeightFormatDirective();
    expect(directive).toBeTruthy();
  });
});
