import { NumberOnlyFormatDirective } from './number-only-format.directive';

describe('NumberOnlyFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberOnlyFormatDirective();
    expect(directive).toBeTruthy();
  });
});
